{#
/**
 * @file
 * Default theme implementation for page titles.
 *
 * Available variables:
 * - title_attributes: HTML attributes for the page title element.
 * - title_prefix: Additional output populated by modules, intended to be
 *   displayed in front of the main title tag that appears in the template.
 * - title: The page title, for use in the actual content.
 * - title_suffix: Additional output populated by modules, intended to be
 *   displayed after the main title tag that appears in the template.
 *
 * @ingroup templates
 *
 * @see template_preprocess_page_title()
 */
#}
{% if banner %}
  {% if title %}
    <div class="row banner banner-type-{{ banner.type }}" style="{{ banner.style }}">
      <div class="container">
        <h1{{ title_attributes.addClass('page-header') }}>{{ title }}</h1>
        {% if banner.subtitle %}
          <div class="subtitle">
            {{ banner.subtitle|raw }}
          </div>
        {% endif %}
        {% if banner.byline %}
          <div class="byline">
            {{ banner.byline|raw }}
          </div>
        {% endif %}
      </div>
      {% if banner.items %}
        <div class="banner-background" style="background-color:{{ banner.background_color }}">
          {% for item in banner.items %}
            <div class="layer-{{ loop.index }}">
              {{ item }}
            </div>
          {% endfor %}
        </div>
      {% endif %}
    </div>
  {% endif %}
{% else %}
  <div class="container">
    <h1{{ title_attributes.addClass('page-header') }}>{{ title }}</h1>
  </div>
{% endif %}
